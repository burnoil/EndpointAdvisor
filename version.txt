Get-ChildItem -Path HKLM:\SOFTWARE -Recurse -ErrorAction SilentlyContinue | Where-Object { $_.PSChildName -like '*FolderName*' }

Get-ChildItem -Path HKLM:\SOFTWARE -Recurse -ErrorAction SilentlyContinue |
    Where-Object { $_.PSChildName -like '*FolderName*' } |
    Select-Object -ExpandProperty Name |
    Out-File "C:\Temp\RegistrySearch.txt" -Encoding UTF8

