if exists file "C:\Path\To\YourScript.ps1" then
  (
    (item 1 of substrings separated by (character 34 as string) of it) of
      (following text of first "=" of
        lines whose (it contains "$ScriptVersion") of file "C:\Path\To\YourScript.ps1")
  )
else
  "N/A"
